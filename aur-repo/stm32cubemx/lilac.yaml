maintainers:
  - github: taotieren

pre_build_script: aur_pre_build(maintainers='laserk3000')

post_build_script: aur_post_build

repo_depends:
  - archlinux-java-run

update_on:
  - source: aur
    aur: stm32cubemx
