# maintainers:
#   - github: taotieren
#     email: admin@taotieren.com
# pre_build_script: aur_pre_build(maintainers=['nadezdacechova','kpcyrd'])
# post_build: aur_post_build
# update_on:
#   - source: aur
#     aur: python-affine
#   - alias: python

maintainers:
  - github: taotieren
    email: admin@taotieren.com
build_prefix: aur-repo-x86_64
pre_build_script: |
  update_pkgver_and_pkgrel(_G.newver)
post_build_script: |
  git_pkgbuild_commit()
  # update_aur_repo()
# repo_depends:
#   - 
update_on:
  - source: github
    github: rasterio/affine
    use_max_tag: true
    # prefix: v
  - alias: python
