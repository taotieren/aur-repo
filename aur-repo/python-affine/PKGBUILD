# Maintainer: Kyle Manna <kyle[at]kylemanna[d0t]com>
# Maintainer: taotieren <admin@taotieren.com>

_pkgname=affine
pkgname=python-${_pkgname}
pkgver=3.0.1
pkgrel=1
pkgdesc="Python package for matrices describing affine transformation of the plane"
url='https://github.com/rasterio/affine'
makedepends=('git'
             'python-build'
             'python-installer'
             'python-wheel'
             'python-flit-core'
            )
depends=('python'
    'python-attrs'
)
optdepends=()
license=('BSD-3-Clause')
arch=('any')
source=("${_pkgname}::git+${url}.git#tag=${pkgver}")
sha256sums=('aa7bfaa0525aa84f2f139ad72f7b3af50500e58fdb8ea8465070ec306c6e5763')

prepare() {
    git -C "${srcdir}/${_pkgname}" clean -dfx
    cd "${srcdir}/${_pkgname}"
    sed -i 's|<4|<5|' pyproject.toml
}

build() {
    cd "$srcdir/${_pkgname}"
    python -m build --wheel --no-isolation
}

package() {
    cd "$srcdir/${_pkgname}"

    python -m installer --destdir="$pkgdir" dist/*.whl

    install -Dm644 -t "$pkgdir/usr/share/licenses/$pkgname" LICENSE.txt
}
