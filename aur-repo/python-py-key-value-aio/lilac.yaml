maintainers:
  - github: taotieren
    email: admin@taotieren.com
build_prefix: aur-repo-x86_64
pre_build_script: |
  update_pkgver_and_pkgrel(_G.newver)
  run_cmd(['python', 'gen_meta.py'])
  run_protected(["updpkgsums"])
  
post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()
repo_depends:
  - python-beartype
update_on:
  - source: pypi
    pypi: py-key-value-aio
  - alias: python
