maintainers:
  - github: taotieren
    email: admin@taotieren.com
pre_build_script: |
  aur_pre_build(maintainers=['andrejr', 'GPereira'])
  add_makedepends(['python-wrapt'])
post_build: aur_post_build
# repo_depends:
#   -
update_on:
  - source: aur
    aur: python-inventree
  - alias: python
