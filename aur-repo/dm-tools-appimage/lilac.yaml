maintainers:
  - github: taotieren
    email: admin@taotieren.com
pre_build_script: |
  update_pkgver_and_pkgrel(_G.newver)
post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()
# repo_depends:
#   -
update_on:
  - source: cmd
    cmd: "curl -sL 'https://api.github.com/repos/dmBots/motor-debugging-tool/contents/Linux/x86_64' | grep -oE 'DMTool-v[0-9.]+-x86_64\\.AppImage' | grep -oE 'v[0-9.]+' | tr -d 'v'"
