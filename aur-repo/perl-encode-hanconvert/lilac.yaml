maintainers:
  - github: taotieren
    email: admin@taotieren.com
# pre_build_script: |
#   update_pkgver_and_pkgrel(_G.newver)
# post_build_script: |
#   git_pkgbuild_commit()
#   update_aur_repo()
# update_on:
#   - source: cpan
#     cpan: Encode-HanConvert
#   - alias: perl

pre_build_script: |
  aur_pre_build(maintainers=['taotieren'])
post_build_script: |
  aur_post_build()

update_on:
  - source: aur
    aur: perl-encode-hanconvert
  - alias: perl
