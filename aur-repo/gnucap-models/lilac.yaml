maintainers:
  - github: taotieren
pre_build_script: |
  aur_pre_build(maintainers=['d_adler'])
  add_makedepends(['git'])
post_build: aur_post_build
repo_depends:
  - gnucap-git
update_on:
  - source: aur
    aur: gnucap-models
