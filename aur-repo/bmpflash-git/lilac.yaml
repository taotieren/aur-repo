# maintainers:
#   - github: taotieren
#     email: admin@taotieren.com
# pre_build_script: aur_pre_build(maintainers=['perigoso'])
# post_build: aur_post_build
# # repo_depends:
# #   -
# update_on:
#   - source: aur
#     aur: bmpflash-git

maintainers:
  - github: taotieren
    email: admin@taotieren.com

pre_build: vcs_update

post_build_script: |
  git_pkgbuild_commit()
#  update_aur_repo()
update_on:
  - source: gitea
    gitea: blackmagic-debug/bmpflash
    host: codeberg.org