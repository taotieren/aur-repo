maintainers:
  - github: taotieren
    email: admin@taotieren.com

pre_build_script: aur_pre_build(maintainers=['taotieren', 'Misaka13514', 'lilac'])

post_build_script: |
  aur_post_build
  git_pkgbuild_commit()

update_on:
  - source: aur
    aur: lceda-pro-bin

# maintainers:
#   - github: taotieren
#     email: admin@taotieren.com
#
# pre_build_script: |
#   update_pkgver_and_pkgrel(_G.newver)
#
# post_build_script: |
#   git_pkgbuild_commit()
#   update_aur_repo()
#
# update_on:
#   - source: regex
#     url: 'https://lceda.cn/page/download?src=index'
#     regex: 'lceda-pro-linux-x64-([\d.]+).zip'
#   - source: manual
#     manual: 1
