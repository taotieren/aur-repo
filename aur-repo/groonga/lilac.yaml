maintainers:
  - github: taotieren
    email: admin@taotieren.com
build_prefix: aur-repo-x86_64
pre_build_script: aur_pre_build(maintainers=['cosmo0920'])
post_build: aur_post_build
# repo_depends:
#   - mecab-git
#   - mecab-ipadic
update_on:
  - source: aur
    aur: groonga
# - source: regex
#   url: https://groonga.org/
#   regex: 'groonga-([\d.]+).tar.gz'
