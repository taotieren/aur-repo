maintainers:
  - github: taotieren
pre_build_script: |
  aur_pre_build(maintainers=['felixs'])
  add_makedepends(['gnucap-modelgen-verilog-git'])
post_build: aur_post_build
repo_depends:
  - gnucap-git
  - numdiff
update_on:
  - source: aur
    aur: gnucsator-git
