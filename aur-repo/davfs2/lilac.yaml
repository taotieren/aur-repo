maintainers:
  - github: taotieren
pre_build_script: |
  aur_pre_build(maintainers=['androw'])
  add_makedepends(['pkgconf', 'neon'])
post_build: aur_post_build
update_on:
  - source: aur
    aur: davfs2
