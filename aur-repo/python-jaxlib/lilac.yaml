maintainers:
  - github: taotieren
pre_build_script: |
  aur_pre_build(maintainers='daskol')
  add_makedepends(['git', 'llvm'])
post_build: aur_post_build
# repo_depends:
#   -
update_on:
  - source: aur
    aur: python-jaxlib
    lilac_throttle: 30d
  - alias: python
