maintainers:
  - github: taotieren

pre_build_script: aur_pre_build(maintainers=['SelfRef'])

post_build: aur_post_build

repo_depends:
  - python-configparser

update_on:
  - source: aur
    aur: python-exscript
  - alias: python
