maintainers:
  - github: taotieren
    email: admin@taotieren.com
pre_build: vcs_update
post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()
update_on:
  - source: vcs
    vcs: git+https://git.code.sf.net/p/blt/src
