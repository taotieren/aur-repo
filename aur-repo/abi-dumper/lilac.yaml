maintainers:
  - github: taotieren

pre_build_script: aur_pre_build(maintainers=['mbloms'])

post_build: aur_post_build

repo_depends:
  - vtable-dumper

update_on:
  - source: aur
    github: abi-dumper
