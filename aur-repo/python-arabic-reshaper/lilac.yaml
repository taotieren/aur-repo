maintainers:
  - github: taotieren
    email: admin@taotieren.com
pre_build_script: |
  aur_pre_build(maintainers=['lapsus'])
  run_cmd(['curl', '-L', '-o', 'PKGBUILD', 'https://aur.archlinux.org/cgit/aur.git/plain/PKGBUILD?h=python-arabic-reshaper'])

  run_cmd(['sed', '-i', 's/check/_check/', 'PKGBUILD'])

  run_cmd(['sed', '-i', 's/makedepends=(/makedepends=(python-hatchling /', 'PKGBUILD'])

  run_cmd(['updpkgsums'])
post_build: aur_post_build
update_on:
  - source: aur
    aur: python-arabic-reshaper
  - alias: python
