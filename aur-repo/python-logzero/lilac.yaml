maintainers:
  - github: taotieren
    email: admin@taotieren.com
# build_prefix: aur-repo-x86_64
pre_build_script: aur_pre_build(maintainers=['greyltc'])
post_build: aur_post_build
# repo_depends:
#   - lief
update_on:
  - source: aur
    aur: python-logzero
# - alias: python
