maintainers:
  - github: taotieren
    email: admin@taotieren.com
pre_build_script: aur_pre_build(maintainers=['Foucault'])
post_build: aur_post_build
# repo-depends:
#   - fxload
update_on:
  - source: aur
    aur: linux-gpib
