maintainers:
  - github: taotieren
    email: admin@taotieren.com
pre_build_script: |
  aur_pre_build(maintainers=['pmattern'])
  add_depends(['pciutils', 'sqlite', 'sh', 'perl', 'glibc'])
  add_makedepends(['autoconf', 'automake', 'pkgconf'])
post_build: aur_post_build
# repo_depends:
#   -
update_on:
  - source: aur
    aur: rasdaemon-git
