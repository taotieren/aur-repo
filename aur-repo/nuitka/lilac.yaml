maintainers:
  - github: taotieren
    email: admin@taotieren.com
pre_build_script: aur_pre_build(maintainers='txtsd')

# run_cmd(['curl', '-L', '-o', 'PKGBUILD', 'https://aur.archlinux.org/cgit/aur.git/plain/PKGBUILD?h=nuitka'])

# for line in edit_file('PKGBUILD'):
#   if line.startswith('check() {'):
#     line = "_check() {"
#   print(line)

# run_cmd(['updpkgsums'])

post_build: aur_post_build
# repo_depends:
#   -
update_on:
  - source: aur
    aur: nuitka
  - alias: python

# pre_build_script: |
#   update_pkgver_and_pkgrel(_G.newver)
# post_build_script: |
#   git_pkgbuild_commit()
# # update_aur_repo()
# update_on:
#   - source: github
#     github: Nuitka/Nuitka
#     use_max_tag: true
