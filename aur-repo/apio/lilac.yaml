maintainers:
  - github: taotieren
    email: admin@taotieren.com

build_prefix: aur-repo-x86_64

pre_build_script: aur_pre_build(maintainers='Xesxen')

post_build: aur_post_build

repo_depends:
  - python-blackiceprog-git

update_on:
  - source: aur
    aur: apio
  - alias: python
