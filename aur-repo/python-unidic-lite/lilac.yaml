maintainers:
  - github: taotieren
    email: admin@taotieren.com
pre_build_script:  aur_pre_build(maintainers='skeilnet')
#  run_cmd(['curl', '-L', '-o', 'PKGBUILD', 'https://aur.archlinux.org/cgit/aur.git/plain/PKGBUILD?h=python-unidic-lite'])
#  run_cmd(['sed', '-i', 's/00/1/', 'PKGBUILD'])
#  run_cmd(['updpkgsums'])
post_build: aur_post_build
# repo_depends:
#   -
update_on:
  - source: aur
    aur: python-unidic-lite
  - alias: python
