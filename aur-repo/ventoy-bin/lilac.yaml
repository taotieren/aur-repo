maintainers:
  - github: taotieren

build_prefix: extra-$(uname -m)

pre_build_script: aur_pre_build(maintainers=['DuckSoft', 'KokaKiwi', 'yochananmarqos', 'Kr1ss'])

post_build: aur_post_build

update_on:
  - sourec: github
    github: ventoy/Ventoy
    use_max_tag: true
    prefix: v

  - source: aur
    aur: ventoy-bin
