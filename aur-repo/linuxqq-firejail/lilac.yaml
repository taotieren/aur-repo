maintainers:
  - github: taotieren

build_prefix: extra-x86_64

pre_build_script: aur_pre_build(maintainers='beautyyuyanli')

post_build: aur_post_build

repo_depends:
  - linuxqq

update_on:
  - source: aur
    aur: linuxqq-firejail
