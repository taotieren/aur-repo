maintainers:
  - github: taotieren
    email: admin@taotieren.com
pre_build: vcs_update
pre_build_script: aur_pre_build(maintainers=['mikesd', 'sebo'])
post_build: aur_post_build
repo_depends:
  - icestorm-git
update_on:
  - source: aur
    aur: arachne-pnr-git
