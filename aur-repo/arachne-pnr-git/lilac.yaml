maintainers:
  - github: taotieren

pre_build_script: aur_pre_build(maintainers='mikesd')

post_build: aur_post_build

update_on:
  - source: github
    github: cseed/arachne-pnr
  - source: aur
    aur: arachne-pnr-git
