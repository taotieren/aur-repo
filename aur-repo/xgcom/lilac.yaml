maintainers:
  - github: taotieren
pre_build_script: |
  update_pkgver_and_pkgrel(_G.newver)
post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()
repo_depends:
  - vte-legacy
update_on:
  - source: github
    github: helight/xgcom
    use_max_tag: true
    # prefix: v
