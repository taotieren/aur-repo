maintainers:
  - github: taotieren
pre_build: vcs_update
post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()
repo_depends:
  - libserialport-git
update_on:
  - source: github
    github: sipeed/sigrok_slogic
# use_max_tag: true
# prefix: v
