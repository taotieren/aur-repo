# maintainers:
#   - github: taotieren
# pre_build_script: aur_pre_build(maintainers=['zxp19821005','taotieren'])
# post_build: aur_post_build
# update_on:
#   - source: aur
#     aur: ttf-alibabasans-puhuiti3-fonts

maintainers:
  - github: taotieren
pre_build_script: |
  update_pkgver_and_pkgrel(_G.newver)
post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()

update_on:
  - source: regex
    url: https://www.alibabafonts.com/#/font
    regex: 'AlibabaPuHuiTi-([\d.]+).zip'
