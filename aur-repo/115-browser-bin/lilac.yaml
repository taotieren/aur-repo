maintainers:
  - github: taotieren
pre_build_script: |
  update_pkgver_and_pkgrel(_G.newver)
post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()
update_on:
  - source: regex
    url: "https://down.115.com/client/115pc/lin/"
    regex: '/115br_v([\\d.]+)\\.deb'
