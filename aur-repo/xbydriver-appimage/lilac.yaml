maintainers:
  - github: taotieren

pre_build_script: |
  update_pkgver_and_pkgrel(_G.newver)

post_build: |
  git_pkgbuild_commit()
  update_aur_repo()

update_on:
  - source: github
    github: gaozhangmin/aliyunpan
    use_latest_release: true
    # use_max_tag: true
    prefix: v
