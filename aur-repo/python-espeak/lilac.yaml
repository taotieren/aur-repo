maintainers:
  - github: taotieren

pre_build_script: aur_pre_build(maintainers=['vinipsmaker'])

post_build: aur_post_build

repo_depends:
  - espeak

update_on:
  - source: aur
    aur: python-espeak
  - alias: python
