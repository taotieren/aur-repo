maintainers:
  - github: taotieren
    email: admin@taotieren.com

pre_build_script: aur_pre_build(maintainers='mijailr')

post_build: aur_post_build

# repo_depends:
#   - lha

update_on:
  - source: aur
    aur: wd719x-firmware
