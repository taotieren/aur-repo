maintainers:
  - github: taotieren

pre_build_script: aur_pre_build(maintainers=['Rubo'])

post_build: aur_post_build

repo_depends:
  - python-polib

update_on:
  - source: aur
    aur: linkchecker
  - alias: python
