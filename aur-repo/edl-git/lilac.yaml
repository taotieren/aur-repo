maintainers:
  - github: taotieren

pre_build_script: aur_pre_build(maintainers=['SelfRef'])

post_build: aur_post_build

repo_depends:
  - python-exscript

update_on:
  - source: github
    github: bkerler/edl
  - source: aur
    aur: edl-git
  - alias: python
