maintainers:
  - github: taotieren
pre_build_script: aur_pre_build(maintainers=['Zes4Null'])
# add_makedepends([''])
post_build: aur_post_build
# repo_depends:
#   - llvm16-lib
#   - clang16
#   - lld16
#   - llvm16
update_on:
  - source: aur
    aur: openvaf
  - alias: python
